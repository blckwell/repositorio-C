#include <stdio.h>

struct lista {
    int num;
    struct lista *proximo;
};

int main () {

    struct lista *proxima;
    struct lista celula;

    proxima = (struct proxima *)*malloc(sizeof (struct proxima));




    return 0;
}